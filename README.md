# Currency Converter

Веб-приложение для конвертации валют с актуальными курсами обмена в реальном времени.

## 🚀 Возможности

- 💱 Конвертация между различными валютами
- 📊 Актуальные курсы валют из ExchangeRate-API
- 🔄 Двусторонний обмен: можно вводить сумму в любой валюте
- ⚡ Реактивное обновление результатов с использованием MobX
- 🎨 Современный и интуитивный интерфейс
- 📱 Адаптивный дизайн

## 🛠️ Технологический стек

- **React 19** - UI библиотека
- **TypeScript** - типизированный JavaScript
- **MobX** - управление состоянием
- **Vite** - сборщик и dev-сервер
- **ExchangeRate-API** - API для получения курсов валют
- **Changesets** - управление версиями и релизами

## 📦 Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/electronstx/currency-converter.git
cd currency-converter
```
2. Установите зависимости:
```bash
pnpm install
```

## ⚙️ Конфигурация

Создайте файл `.env` в корне проекта и добавьте ваш API ключ:

```bash
VITE_EXCHANGE_RATE_API_KEY=your_api_key_here
```
Получить бесплатный API ключ можно на [ExchangeRate-API](https://www.exchangerate-api.com/).

## 🎯 Использование

### Запуск в режиме разработки

```bash
pnpm dev
```
Приложение будет доступно по адресу `http://localhost:5173`

### Сборка для продакшена

```bash
pnpm build
```

### Предпросмотр продакшен сборки

```bash
pnpm preview
```

### Линтинг
```bash
pnpm lint
```

#### Создание changeset
```bash
pnpm changeset
```
Создаёт файл changeset с описанием изменений для следующего релиза.

#### Обновление версий
```bash
pnpm version
```
Обновляет версии пакетов на основе созданных changesets.

#### Публикация релиза
```bash
pnpm release
```
Публикует пакет (если проект публикуется в npm).

## 📁 Структура проекта

```
currency-converter/
├── .changeset/ # Конфигурация Changesets
│ └── config.json
├── src/
│ ├── api/
│ │ └── currencyApi.ts # API для получения курсов валют
│ ├── components/
│ │ ├── Converter/
│ │ │ ├── Converter.tsx # Основной компонент конвертера
│ │ │ └── Converter.css
│ │ └── CurrencyInput/
│ │ ├── CurrencyInput.tsx # Компонент ввода валюты
│ │ ├── CurrencyInput.css
│ │ └── types.ts
│ ├── store/
│ │ ├── CurrencyStore.ts # MobX store для управления состоянием
│ │ └── CurrencyStoreContext.tsx # React Context для store
│ ├── App.tsx # Главный компонент приложения
│ ├── App.css
│ ├── index.css
│ └── main.tsx # Точка входа
├── index.html
├── package.json
├── vite.config.ts
└── tsconfig.json
```

## 🔧 Как это работает

1. При загрузке приложения автоматически загружаются актуальные курсы валют из ExchangeRate-API
2. Пользователь вводит сумму в первой валюте
3. Выбирает исходную и целевую валюты из выпадающих списков
4. Результат конвертации автоматически обновляется при изменении суммы или валют
5. Вторая валюта доступна только для чтения и показывает результат конвертации